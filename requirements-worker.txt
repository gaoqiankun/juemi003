-r requirements.txt

# Real-mode helper/runtime packages used on the GPU server.
# A CUDA-compatible torch build and the TRELLIS2 runtime still need to be
# installed explicitly per the target server image and driver stack.
huggingface_hub>=0.28,<1.0
accelerate>=1.3,<2.0
safetensors>=0.5,<1.0

# Recommended follow-up installs on a CUDA 12.4 Linux server:
#   python -m pip install \
#     torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 \
#     --index-url https://download.pytorch.org/whl/cu124
#   git clone -b main --recursive https://github.com/microsoft/TRELLIS.2.git /opt/TRELLIS.2
#   export PYTHONPATH=/opt/TRELLIS.2:$PYTHONPATH
