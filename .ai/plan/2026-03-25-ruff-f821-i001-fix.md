# Ruff F821 + I001 修复
Date: 2026-03-25
Status: done

Date / Status: 2026-03-25 / done / Commits: N/A（按 AGENTS.md 要求未执行 commit）
## Goal
修复 Ruff 报告中的 3 处 F821（undefined name）真实代码问题，并自动修复 19 处 I001（import 排序）。

## Key Decisions
- 仅处理 `F821` 与 `I001`，不处理 `E402` 与 `C901`。
- F821 按“先定位根因、再最小改动修复”执行。
- I001 使用 Ruff `--fix` 自动修复，避免手工调整引入偏差。

## Changes
- 已修复：`tests/test_api.py` 中 3 处 `F821 Undefined name 'Any'`。
- 已执行：`ruff check . --select I001 --fix`，自动修复 19 处 import 排序问题。
- 已验收：
  - `.venv/bin/ruff check . --select F821` → All checks passed
  - `.venv/bin/ruff check . --select I001` → All checks passed
  - `.venv/bin/python -m pytest tests -q` → 163 passed

## Notes
- 基线测试：`.venv/bin/python -m pytest tests -q` = 163 passed。
- 本次按任务要求未处理：`E402`（39）与 `C901`（8）。
